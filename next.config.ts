import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com", "imgs.search.brave.com", "abhi-psi.s3.eu-north-1.amazonaws.com", "images.indianexpress.com", "static.india.com", "sscportal.in", "encrypted-tbn0.gstatic.com"],
  },
};

export default nextConfig;
